import Navbar from "@/components/Navbar";

export const metadata = {
  title: "Article title",
  description: "Article description",
  date: "2019-01-01",
  tags: ["example", "test"],
};

<Navbar title={metadata.title} tags={metadata.tags} />

## TEST